{
  "name": "arcanelab-langchain",
  "private": false,
  "version": "1.0.1",
  "description": "",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "repository": {
    "type": "git",
    "url": "https://github.com/arcanelabai/langchain-js"
  },
  "scripts": {
    "prepublish": "npm run build",
    "build": "tsc",
    "test": "echo \"Hello World\" && exit 0",
    "ts": "ENV=local ts-node",
    "clean:build": "rm -rf src/**/*.js && rm -rf src/**/*.d.ts && rm -rf src/**/*.js.map"
  },
  "devDependencies": {
    "@types/node": "^20.2.4",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.4"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.4.0",
    "langchain": "^0.0.133"
  }
}
